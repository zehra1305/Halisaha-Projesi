import 'package:flutter/material.dart';

class MesajlasmaScreen extends StatelessWidget {
  const MesajlasmaScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Mesajlaşma')),
      body: const Center(child: Text('Mesajlaşma ekranı buraya gelecek.')),
    );
  }
}
